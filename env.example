# =============================================================================
# Open-Source-KI-Stack Environment Configuration
# =============================================================================
# Kopieren Sie diese Datei zu .env und passen Sie die Werte an Ihre IONOS-Konfiguration an
# cp env.example .env

# =============================================================================
# OpenWebUI Konfiguration
# =============================================================================
# Generieren Sie einen sicheren Secret Key: openssl rand -hex 32
WEBUI_SECRET_KEY=your_secure_secret_key_here_minimum_32_characters
# Name der Anwendung, der in der UI angezeigt wird
WEBUI_NAME=Company AI Hub


# =============================================================================
# IONOS PostgreSQL Managed Database Konfiguration
# =============================================================================
# Hostname Ihrer IONOS PostgreSQL-Instanz (z.B. pg-oemrj7r0kfjipg97.postgresql.de-txl.ionos.com)
IONOS_DB_HOST=your_ionos_db_host.ionos.com
# PostgreSQL Port (normalerweise 5432)
IONOS_DB_PORT=5432
# Datenbankname (wird automatisch erstellt, wenn nicht vorhanden)
IONOS_DB_NAME=openwebui
# Benutzername für die PostgreSQL-Datenbank
IONOS_DB_USER=your_db_username
# Passwort für die PostgreSQL-Datenbank
IONOS_DB_PASSWORD=your_secure_db_password

# =============================================================================
# IONOS Redis In-Memory DB Cluster Konfiguration
# =============================================================================
# Hostname Ihrer IONOS Redis-Instanz (z.B. 10.7.226.3 für private IP)
IONOS_REDIS_HOST=your_ionos_redis_host.ionos.com
# Redis Port (normalerweise 6379)
IONOS_REDIS_PORT=6379
# Benutzername für Redis (z.B. redis_nutzer)
IONOS_REDIS_USER=your_redis_username
# Passwort für Redis (z.B. Teleporter2024!)
IONOS_REDIS_PASSWORD=your_secure_redis_password

# =============================================================================
# Optional: LDAP Konfiguration (auskommentiert)
# =============================================================================
# LDAP_BIND_PASSWORD=your_ldap_password

# =============================================================================
# Beispiel-Konfiguration für Staging-Umgebung:
# =============================================================================
# WEBUI_SECRET_KEY=xxx
# WEBUI_NAME=Staging AI Hub
# IONOS_DB_HOST=pg-xxxx.postgresql.de-txl.ionos.com
# IONOS_DB_PORT=5432
# IONOS_DB_NAME=openwebui
# IONOS_DB_USER=xxxx
# IONOS_DB_PASSWORD=xxxx
# IONOS_REDIS_HOST=10.7.xxxxxx
# IONOS_REDIS_PORT=6379
# IONOS_REDIS_USER=xxxx
# IONOS_REDIS_PASSWORD=xxxxx

# Microsoft Entra ID siehe Openwebui docs